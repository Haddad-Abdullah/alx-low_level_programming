#include "main.h"

/**
 * print_most_numbers - prints the numbers, from 0 to 9 but 2 and 4
 *
 */

void print_most_numbers(void)
{
	char i;

	if (i != 50 && i != 52)
	{
		for (i = 48; i < 58;)
		{
			_putchar(i);
			_putchar('\n');
		}
		i++
	}
}
