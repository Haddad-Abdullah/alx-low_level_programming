#include "main.h"

/*
 * print_last_digit - Entry point
 *
 * Description: 'printing last digit in numerics'
 *
 * Return: n
 */

int print_last_digit(int n)
{
	char C[] = int a;
	for (i = 0; i <= a; i++);
	return (C[i]);
}
